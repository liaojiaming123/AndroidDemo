<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.weiyu.demorecycle">

    <application
        android:allowBackup="true"
        android:name=".MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".drag.DragActivity"></activity>
        <activity android:name=".decoration.DecorationActivity" />
        <activity android:name=".loader.LoaderActivity" />
        <activity android:name=".header.HeaderActivity" />
        <activity android:name=".click.InterfaceClickActivity" />
        <activity android:name=".click.ClicklActivity" />
        <activity android:name=".stagger.StaggerActivity" />
        <activity android:name=".grid.GridActivity" />
        <activity android:name=".horizontal.HorizontalActivity" />
        <activity android:name=".vertical.VerticalActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>